# version: "3.8"

# services:
#   app:
#     image: aannaassalam/shiftcare-jr:latest
#     platform: linux/amd64
#     build:
#       context: .
#       dockerfile: Dockerfile
#     ports:
#       - "14009:14009"
#     restart: always

version: "3.8"

services:
  web:
    image: mullickzaidkhanjb/chatn8n:latest
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "14009:14009"
    environment:
      - NODE_ENV=production
    restart: always
